apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qserv-data-qserv-dev-worker-12
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  selector:
    matchLabels:
      pvc_name: qserv-data-qserv-dev-worker-12
  storageClassName: qserv-local-storage
  volumeMode: Filesystem
