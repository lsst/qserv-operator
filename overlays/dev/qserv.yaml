apiVersion: qserv.lsst.org/v1alpha1
kind: Qserv
metadata:
  name: qserv
spec:
  devel:
    corepath: "/tmp/coredump"
  replication:
    debug: "all"
  worker:
    replicas: 3
  xrootd:
    replicas: 4
