sudo: required
language: go

go
- 1.11.12

before_script:
  - git clone --depth 1 -b "master" --single-branch https://github.com/fjammes/kind-travis-ci.git
  - ./kind-travis-ci/kind/k8s-create.sh

script:
  - ./kind/launch.sh

after_success:
  - echo "Push qserv_deploy image to Docker Hub"
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - ./build-image.sh
